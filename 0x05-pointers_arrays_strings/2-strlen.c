#include "main.h"
#include <stdio.h>
/**
 * _strlen - length of a string
 * @s: a pointer to an int that will be changed/updated
 * Return: Always 0
 */
int _strlen(char *s)
{
	int n;

	n = 0;
	while (s[u] != '\0')
	{
		u++;
	}
	return (u);
}
